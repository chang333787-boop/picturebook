# Firebase service account — 절대 commit 박지 말 것
*adminsdk*.json
*-firebase-adminsdk-*.json
serviceAccountKey*.json

# Node
node_modules/
*.log

# OS
.DS_Store

# ════════════════════════════════════════════════════════════════
# Phase A 박을 때 박을 거 박은 거 박은 박은 환경변수 / secrets
# AI_PHASE_A_USER_PREP_CHECKLIST.md 5장 박힘 (2026-05-21)
# ⚠️ Anthropic API key 박은 거 박은 거 박은 박은 박혀있을 위치 — 절대 commit X
# ════════════════════════════════════════════════════════════════

# Firebase Functions — 환경변수 / 빌드 산출물
functions/node_modules/
functions/.env
functions/.env.local
functions/.env.*.local
functions/.runtimeconfig.json
functions/lib/
functions/.firebase/

# 이중 안전망 — 어떤 위치 박혀있어도 .env 박지 X
**/.env
**/.env.local
**/.env.*.local

# 추가 secrets 패턴
**/*-secret.json
**/*.secret
**/secrets.json

# Claude Code 개인 권한 설정 (사용자별 — commit 박지 X)
.claude/

# IMAGE-S2-8 모델 평가 — 생성 결과/계획(외부 API 산출물·dry-run 산출) commit 금지
scripts/image-s2-model-eval/output/
functions/node_modules
아카이브.zip
*.zip

# 로컬 전용 백업/목업 — commit 금지(2026-07-25 AUDIT-CLOSE-1 실수 재발 방지)
picturebook-repo-full/
picturebook-repo-full 2/
d7-5-theme-stage-mockups/
mockups/
mockup-*.html
database-debug.log
