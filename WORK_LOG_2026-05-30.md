# 작업 로그 — 2026-05-30

## 0. 문서 목적

- `eb808be Document admin mode status` (어제) 이후 진행한 작업 정리
- 새 세션에서 이어받을 수 있도록 상태 고정
- 아직 commit하지 않은 작업의 맥락 보존

---

## 1. 기준 commit

- 마지막 commit: **`eb808be Document admin mode status`** (2026-05-29 14:07)
- 이 문서 박는 시점 working tree: clean
- 이 문서 박은 commit 이후 새 세션에서 작업 이어가기

---

## 2. 어제 commit 이후 진행한 작업 (모두 코드 변경 없음)

### 2-1. 텍스트형 / 무비형 완성도 조사
- viewer-render.js의 `_renderSceneText`, `_renderSceneMovie` 구조 확인
- viewer-edit.js의 `_typeSectionTextHtml`, `_typeSectionMovieHtml` 확인
- 두 모드 모두 골격은 있고 운영 가능 수준으로 판단
- 그림책 모드의 Phase 4 개선(미리보기 contenteditable / 1단 통합 / compact)은 텍스트/무비에 적용되지 않은 상태

### 2-2. 실제 Firebase 데이터 read-only 확인
class_2026_junglim_1 안 34팀의 `viewer-meta/projectType` 분포:

| projectType | 팀 수 | 팀명 |
|---|---|---|
| **text** | **5** | `0001`, `1`, `00000`, `0003`, `0004` |
| picturebook | 5 | `1235`, `0000`, `000000`, `1234`, `00001` |
| **movie** | **1** | `2222` |
| experience | 1 | `1111` |
| null (미선택) | 22 | `유은`, `이시아`, `시아`, `선생님`, `텍스트` 등 |

### 2-3. text 팀 `0001` 데이터 구조 확인
- 2 scenes
- scene 1: type=normal, body 89자, `textTheme=retro`, `textStyle` (color/font/size/weight 모두), `textEffect` (entrance=fade, body=typewriter)
- buttons: 정상 (`다음페이지로` → nextId 2, B 빈 채)
- imageData 없음
- cover scene 없음 → 스크린샷 자동화에서 `.js-cover-start` 클릭 실패

### 2-4. movie 팀 `2222` 데이터 구조 확인
- 7 scenes (scene 6, 7은 ending)
- **모든 scene이 videoUrl 없음** — 영상 없는 movie 상태
- scene 1: title "무비형 연습", imageData 있음 (poster fallback), bodyEnabled=true, captionMode/choiceReveal 없음 (default)
- buttons 정상

### 2-5. Codex read-only 리뷰
- text mode: **mostly ready** — 라우팅/렌더/편집/저장 모두 정상. 그림책 대비 미리보기 직접 입력 없음 (편의성 갭, 블로커 아님)
- movie mode: **mostly ready, 실 사용 위험 ↑** — videoStoragePath reload 시 누락 / Storage·auth·rules 위험
- 블로커: 코드 경로상 없음. verification만 남음
- Red flag:
  - 스크린샷 default가 실 Firebase 박는 위험 (이미 경고 박혀있음)
  - movie `videoStoragePath` reload 시 cleanup 메타데이터 누락
  - movie normalization 주석이 옛 상태로 박혀있음

### 2-6. 자동 스크린샷 생성 (commit 안 함 — 산출물만)
- text 팀 `0001`:
  - `scripts/screenshots/screenshots/2026-05-30-09-20/`
  - 12장 성공 (cover-edit / cover-view / scene-edit / branch-view × 3 viewport)
  - **scene-view 3장 실패** — `.js-cover-start` 없음 (cover scene 없는 작품)
- movie 팀 `2222`:
  - `scripts/screenshots/screenshots/2026-05-30-09-22/`
  - 15장 모두 성공

---

## 3. 다음 단계 (사용자 판단 대기)

### 사용자 시각 확인 필요한 항목
1. text `0001` PNG 확인 — typewriter 효과 / retro 테마 / textStyle 적용 / 버튼
2. movie `2222` PNG 확인 — 영상 없는 placeholder / decision panel 등장 타이밍 / 본문 표시
3. 브라우저 직접 확인 (저장 / 업로드 / 삭제는 금지):
   - `http://localhost:8765/viewer.html?team=0001&classId=class_2026_junglim_1&ptype=text`
   - `http://localhost:8765/viewer.html?team=0001&classId=class_2026_junglim_1&edit=1&from=maker&ptype=text`
   - `http://localhost:8765/viewer.html?team=2222&classId=class_2026_junglim_1&ptype=movie`
   - `http://localhost:8765/viewer.html?team=2222&classId=class_2026_junglim_1&edit=1&from=maker&ptype=movie`

### 4가지 판단 옵션
- **A. text OK** — 마감 전 추가 구현 불필요
- **B. text 보완 필요** — CSS / 편집 패널 최소 수정 후보 정리
- **C. movie OK** — 영상 없는 상태에서도 자연스러움
- **D. movie 보완 필요** — 영상 없는 상태 어색 / 영상 있는 테스트 장면 필요

---

## 4. 안전 정책 (이어받을 때 지킬 것)

- **저장 금지** — 화면 확인만 (입력 / 업로드 / 삭제 / 토글 / 복사 / maker 수정 X)
- **Firebase 쓰기 금지** — read-only 조회만
- **viewer-data.js / maker.html / functions/ / database.rules.json 보호 영역** — 텍스트/무비 완성도 작업은 이 파일들을 건드릴 필요 없음
- **자동 스크린샷 default 위험** — 실 Firebase 작품에 lock/projectType 쓰는 경고 박혀있음. 격리 작품 미설정 상태

---

## 5. 새 세션 시작 시 이어받을 정보

다음 commit 후 새 세션에서 첫 메시지로 박을 내용:

```
가지 프로젝트 작업 이어서.

마지막 commit: <이 문서 박은 commit hash>
참고 문서: WORK_LOG_2026-05-30.md

현재 단계: 텍스트형(0001) / 무비형(2222) 화면 검수
- 자동 스크린샷 이미 생성됨 (text 12장 / movie 15장)
- 사용자 PNG 시각 확인 후 A/B/C/D 판단 대기 중

이어서 사용자 판단 받고 진행.
```

---

**작성 시점**: 2026-05-30
**작성 이유**: 새 세션에서 이어갈 때 맥락 손실 방지
