# 가지 마감 계획 + 전수 감사 결과 (2026-07-25)

> branchstory.co.kr 이전 완료 후 **마감 단계 단일 출처**.
> 감사 방식: 11차원 × 전담 에이전트 코드 감사(52에이전트·도구 399회) + 발견 41건 **건별 적대적 반증** → 확정 35 / 기각 6.
> 추가로 브라우저 실측(연습반 2단계1·시트콤1)과 이미지 전수 육안(9장) 병행.

## 1. 오늘 반영·배포된 것

### A. 클라 13건 — `a54b4e7` (push=라이브)
| # | 내용 | 심각도 |
|---|---|---|
| 0/6 | **그림 표시버전 잠금이 렌더에 미적용** — 잠가도 AI그림 노출·토글은 숨김(글만 잠기는 비대칭). `_getDisplayImageSrc`에 잠금 강제 | high |
| 9 | 부트스트랩이 변형 캐시 로드 전에 학생의 명시 'AI' 선택을 'original'로 **영구 덮어씀** → 캐시 로드 후에만 정리 | med |
| 10/24 | 책장 자동복귀가 무파라미터 진입까지 **납치**(진입 폼 사용 불가) + 늦은 응답이 감상 중 화면 덮음 → reload 한정+ifIdle | med |
| 8 | 1·2단계 잠금 누수 — ⋯펼침 안 선택지 라벨·진엔딩 토글이 관문 밖 → 함수 관문+CSS 이중 방어 | med |
| 17/19/20 | 나침반: 맞춤보기 교차 오염(질문키별 single-flight)·완료 더블클릭 이중 실행(busy 선점)·직접적기 포커스 강탈(재렌더 생략) | med |
| 21 | 입장카드 선택 모달에 전 모둠 **PIN 평문 노출** → •••• 마스킹(인쇄물에만 실PIN) | med |
| 23 | 그림책 인쇄 재진입 무가드(그림 대기 중 재클릭=그림 누락 인쇄) → busy 가드+자기 root만 정리 | med |
| 27 | 1단계 그림 자동생성 플래그가 새로고침에 유실되면 **그림이 영영 안 만들어짐** → 부팅 소비처 추가 | med |
| 16/32 | 버스터 구멍 2곳 — 나침반 지연번들(viewer-edit.js `rosefirst1` 고정)·tokens-warm.css → bump-busters 커버리지 편입(결합해시) | med/low |
| 33 | 인쇄 도우미 글자 100% 복원이 저장에서 탈락 → 1.0도 저장 | low |

### B. functions 7건 — `81750c0` (배포: 이 문서 작성 시점 진행/완료)
| # | 내용 |
|---|---|
| 1/5/7/25 | **joinTeamMembership**: 구학급(legacy_open 폴백)에서 교사 발급 account 팀이 전면 차단되던 것 — account 있으면 teacher_managed와 동일 검증(locked·pin). 자가등록 차단 유지 |
| 2 | **teacherScriptDraft**: testMode·origin·killswitch·전역캡 게이트 추가(비용 방어선 공통화) |
| 12/26 | **protagonistDesc 안전 스캔**: 학생 자유 텍스트가 이미지 프롬프트에 무스캔 직주입되던 것 — 걸리면 특징 글만 버리고 생성 진행 |
| 13 | **adminReset**: 이전 주인공(protagonistRef/Desc) 잔재 삭제 — 새 이야기에 옛 주인공 강제 주입 차단 |
| 11 | **studentStoryDraft**: 전역 일일캡 500(이미지와 대칭) |
| 30 | generateStoryImages 전역캡 transaction 실패 시 팀 쿼터 반납(환불 대칭) |
| 31 | AI 실패 8곳 내부 오류 원문 노출 → 고정 한글 문구 |

### C. 그 외 오늘 완료
- **SCRIPT-DRAFT-HARDEN-1**(`bcbbd24`·배포): 대본 저장 rules $other 봉쇄+에러문구.
- **ADAPTIVE-CHOICES-CACHE**(`39531d2`·배포): 나침반 맞춤보기 서버 캐시(질문별 1슬롯·프롬프트 해시 무효화).
- **NUL-BYTE-FIX**(`765b2b4`): thought-compass-ui.js 소스에 박힌 리터럴 0x00 — grep류 도구 전체가 이 파일을 바이너리로 건너뛰던 지뢰.
- **왕관 재생성**: '2단계1' 장면 3·9 주인공(왕관) 소실 발견(9장 중 7장만 일관) → 재생성·DB 반영, 왕관 복원 육안 확인.

## 2. 보류 9건 (사유와 함께 — 다음 결정 대상)

### 🔴 rules 익명 쓰기 클러스터 (#3 viewer-meta·#4 scenes·#14 session·#15 레거시 top-level)
아무 익명 로그인이 남의 팀 scenes/viewer-meta(isPublic 포함)/session에 쓸 수 있음.
**= 기존에 알던 SCENES-WRITE-HARDEN-1** (⚠️메모리: HARDEN-1 배포 보류·활성학급 위험·**트리거=학급종료**).
감사가 독립적으로 재발견 → 실재 확인. 방학/학급종료 시점에 HARDEN-1 배포로 일괄 해소 권장.
#15(top-level teams read:true)는 비-classId 레거시 경로 폐기와 함께 정리.

### 🟡 기능 보류
- **#18** 나침반 답 수정 후 옛 후속답이 FOLLOWUP-DIGEST로 초안에 계속 주입 — 후속답 무효화 시점 설계 필요(답 변경 시 해당 질문 후속 삭제가 유력).
- **#22** maker 인쇄 3종(대본·입장카드·설명서) print-doc-unclip 미적용 — 다중 페이지 잘림 가능성. 인쇄 CSS 수술이라 실프린터 검증과 함께.
- **#29** 그림 잠금 'aiS2'인데 일부 장면만 AI면 원본/AI 혼재가 표식 없이 노출(low·정보 표시 문제).
- **#28** 선형 그리드 탭 중 에코 재렌더로 클릭 유실(low·간헐) — 입력 처리 코드라 마감 직전 수정 위험>이득.
- **#34** 인쇄 이미지 4초 타임아웃 시 빈칸(low) — 자리표시 이미지 추가 후보.
- **(추가 관찰)** 🧹 계정만 삭제가 legacy pin 노드 잔존 팀에선 재입장을 못 막는 edge(구학급 한정).

### 기각 6건(오탐 확인) — 검증 로그는 워크플로 저널 참조.

## 3. 이미지 일관성 실측 결론 (사용자 보고용)
- PROTAG-DESC(특징 글 게이트)는 **강한 개선이지만 보장이 아님**: '2단계1' 재생성 9장 기준 왕관 유지 7/9 → 재롤로 9/9.
- 남은 실패 모드: ① 감정 격한 장면에서 특징 소실(재롤로 해결) ② **인물 수 드리프트**(2명→3명 증식 이따금).
- 운영 가이드: 아이가 재생성 버튼으로 재롤 → 대부분 해결. 반복 실패 장면만 교사가 확인.

## 3-1. 사후 실검증 (같은 날 추가 — 증거)
- ✅ **그림 표시버전 잠금(오늘 최고 심각도) 양방향 실왕복**: 연습반 2단계1에 실제로 잠금을 걸어 확인.
  `original` → 아이가 그린 원본 표시 + 토글바 숨김 / `aiS2` + 기기에 남은 'original' 선택 → **잔존 선택 무시하고 AI 그림 강제**. 테스트 값은 삭제·원상복구 완료.
- ✅ **학생 입장 회귀 없음**: 배포된 joinTeamMembership으로 실입장 → members.lastVerifiedAt 26초 전 갱신 확인.
- ✅ **legacy 폴백 학급 실조사**: 모드 미설정 학급 2개(「우리반 만세」=팀 1개·legacy pin만 / 「연구대회 학급(4학년)」=팀 0).
  위험 케이스(pin과 account가 둘 다 있고 값이 다른 팀)는 프로덕션에 존재하지 않음 → 이번 수정 안전.
  ⚠️ 다만 종전 기록의 "null 폴백=테스트성뿐"은 부정확했음(이름 있는 실학급이었음).
- 관찰(low·기존 동작): 잠금 `aiS2` 첫 렌더에서 변형 캐시 로드까지 수 초간 빈 화면.

### 아직 한 번도 실행되지 않은 것 (배포는 됐음)
서버 6건 — 대본 게이트·특징 글 안전 스캔·리셋 잔재 삭제·초안 전역캡·환불 대칭·에러 문구.
코드 검증과 배포는 끝났으나 **실호출 0**. 다음 수업/생성 때 첫 실행됨.

## 4. 남은 검증 체크리스트 (사용자 동석 필요)
1. **특징칸 게이트 실계정 e2e** — 신규 작품에서 주인공 그리기+특징 글 필수 게이트.
2. **나침반 맞춤보기 실 AI 품질** — 사용자 계정 몇 회 + 이번 캐시 히트 확인(같은 답=재호출 없음).
3. **#25 설명서 실화면 캡처** — 교사 로그인+PII 마스킹.
4. **감상 표시버전 잠금 실왕복** — 이번 수정(C1) 후 '원본' 잠금이 실제로 원본만 보이는지(잠금 설정→친구 계정/시크릿 창).
5. 실프린터 1회(그림책+대본), 실크롬북 1회.

## 5. 마감 기준(정의)
- [x] 도메인 이전 완결(스모크 PASS) + 전기능 울트라코드 검증
- [x] 마감 전수 감사(41건) → 확정 전건 수정 또는 사유 있는 보류
- [ ] §4 동석 검증 1~4
- [ ] HARDEN-1 배포(학급종료 시) — 익명 쓰기 클러스터 종결
- [ ] 심사반(9999) 10월 유지 종료 후 정리

## 부록 — 오늘 커밋 체인
`bcbbd24`(HARDEN-1) → `39531d2`(캐시) → `765b2b4`(NUL) → `a54b4e7`(클라 13) → `81750c0`(functions 7)
※ `111709c`는 백업 사본 오커밋 — 즉시 force-push 교체(현 히스토리에 없음)·.gitignore로 재발 방지.
