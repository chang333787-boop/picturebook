# 그림책 3단계 확장 설계 (PICTUREBOOK-LEVELS) — 2026-07-18

> 상태: **구현 완료 (2026-07-19 야간) — 1·2·3단계 전부 라이브·연습반 실계정 e2e 통과** · 대상 제출: 연구대회
> 승인 결정: 주1 착수 · 말풍선 폴백(하단 고정) 수용 · 1단계=일직선 6~8장면 · 비용 상한=**팀당 총량**
>
> ## 구현 결과 요약 (커밋: `e13d153` 진입 → `c805d91` 2단계 → `8a42fcb` 1단계 → `bfbd512` 타임아웃)
> - **①진입 위계**·**②말풍선 PoC GO(9/10)**·**③2단계**(일직선 잠금+studentStoryDraft 초안·연습반 '이단계테스트' e2e)·**④1단계 풀오토**(studentStoryDraft 완성문+generateStoryImages 자동 그림 9장+말풍선 상단밴드 프리셋+고쳐쓰기 스킵+🔁 장면별 재생성·연습반 '일단계테스트' e2e "별 찾는 작은 로봇")·**⑤팀당 쿼터**(초안 3회·그림 24회) 전부 검증 완료.
> - **유일 설계 편차 — §7.3 나침반 easy 세트 보류**: 질문 세트는 "정확히 10개" 형태검증+AI판정+DEPTH+결과지+3곳 동기화가 얽힌 전 단계 공유 코어라, 세트 신설은 회귀 면적이 커서 후속 트랙으로 분리. 1단계도 v2 10문항 사용(전부 칩 탭으로 답 가능·실측 문제없음). 후속: v3-easy 설계 문서 별도.
> - 실측 비고: 클라 fire-and-forget은 기본 70s deadline 로그를 남겨 timeout 명시(`bfbd512`). 감상에서 그림은 storage fetch 완료 시점에 뜸(빠른 페이지 넘김 직후 순간 공백 가능·재방문 시 정상 — 기존 s2와 동일 특성).
> - NOT_VERIFIED: 실기기(크롬북)·실프린터 인쇄.

---

## 0. 요약

- 진입 화면에서 **그림책을 위계 최상단**으로 올리고, 그림책 자체를 **1·2·3단계**(학년별 AI 비계 사다리)로 확장한다. 텍스트·무비형·친구작품복사는 하단(부차)으로 내린다.
- 새 projectType 3개가 아니라 **`picturebook` + `viewer-meta/picturebookLevel`(1|2|3) 서브필드 하나**. 감상·인쇄·책장·댓글은 세 단계 전부 동일 렌더 → **뷰어/인쇄 무변경 원칙**.
- 유일한 신규 난제 = **말풍선 자동 배치**. 얼굴인식 ML 없이 **그림 구도 통제**(인물 하단·상단 1/3 여백 지시 + 글상자 상단 고정)로 푼다. **2~3일 PoC로 GO/NO-GO** — 이것이 1단계 전체의 관문.
- 함수 변경 2건(글 초안 콜러블·text→image 어댑터)은 **안전 모드**(조사·구현 분리, 배포 전 승인) 대상.

---

## 1. 배경·목적

연구대회 제출 재구성. 현재 진입은 텍스트/그림책/무비형이 **동등**한 카드 그리드인데, 메인은 그림책이다. 그림책을 위계 위에 두고 1~3단계로 확장해 **"AI가 대신하는 양 ↔ 아이 자율"의 사다리**를 대회 서사의 뼈대로 삼는다. 아이는 1→2→3을 거치며 사고를 확장한다(1단계에서 아이가 뭘 안 해도 OK — 사용자 명시).

## 2. 세 단계 정의 — AI 비계 사다리

| 단계 | 대상 | 이름(안) | AI가 하는 것 | 아이가 하는 것 | 구조 |
|---|---|---|---|---|---|
| **1** | 1·2학년 | "나의 동화책을 만들어줘요" | 나침반 easy 답변으로 **글 전체 생성 + 그림 전체 생성(text→image) + 말풍선 자동 배치**. 고쳐쓰기 없음 | 나침반 easy 답하기 · 글자 위치 조정 · 맘에 안 드는 글씨 약간 수정 | **일직선 6~8장면** (권고·§11-③) |
| **2** | 3·4학년 | "도움을 받아 만들어봐요" | 나침반(현행 v2) 내용으로 **장면별 이야기 초안 배치** | **초안을 자기 글로 고침** · 그림은 직접(그리기/업로드/AI마감 현행) · 고쳐쓰기 현행 | **일직선** (분기 잠금) |
| **3** | 4·5·6학년 | "갈림길이 있는 이야기를 스스로의 힘으로" | 현행 그대로 (텍스트1·2, imageS2, 나침반, 고쳐쓰기) | 전부 스스로 | **분기** (현행) |

사다리 서사: 갈림길 이야기를 **받아 읽는 아이(1)** → **도움받아 쓰는 아이(2)** → **갈림길을 만드는 아이(3)**. "갈림길"은 3단계의 이름이자 도달점.

## 3. 아키텍처 결정

**핵심 통찰 (07-18 코드 확인)**: 감상·인쇄·뷰어·책장·댓글은 세 단계 전부 동일한 picturebook 렌더를 쓴다. 단계 차이는 **maker 비계(scaffold)뿐**이다.

- ✅ 채택: `projectType='picturebook'` 유지 + **`viewer-meta/picturebookLevel: 1|2|3`** 서브필드 신설.
- ❌ 기각: projectType 3개 신설 — 뷰어/인쇄/책장/rules/functions의 ptype 분기 전부에 파급. 불필요.
- **레거시 규칙**: 필드 없음(기존 전 작품) = **3단계로 취급**. 마이그레이션 0.
- **뷰어/인쇄 무변경 원칙**: picturebookLevel은 **maker 계열(ui/maker/compass/viewer-edit 게이트)만 읽는다**. viewer-render·print는 읽지 않는다. (1단계 결과물도 렌더 관점에선 "본문+그림+bodyBox 좌표가 채워진 보통 그림책")

## 4. 데이터 모델

```
classes/{classId}/teams/{teamKey}/viewer-meta/
  projectType: "picturebook"        (기존)
  picturebookLevel: 1 | 2 | 3       (신규 · 없으면 3)
```

- **쓰기**: 진입 카드 선택 시 1회 (`selectProjectType` 확장, ui.js:711). projectType 저장 경로(firebase.js:688~694 once 조회→저장)에 편승.
- **변경 정책**: 작품 생성 시 결정·이후 고정 (현행 projectType과 동일 UX). 변경은 교사 리셋(이미지/AI 리셋 계열)으로만 — 단계 전환 시 산출물 정합(자동 그림·초안) 문제를 원천 차단.
- **읽기**: maker 진입 분기·나침반 세트 선택·viewer-edit의 1단계 자동그림 트리거·고쳐쓰기 게이트. **viewer-render/print/책장은 안 읽음**.

## 5. 진입 화면 재구성 (와이어)

anchor: maker.html:2964 `#ptype-grid`(카드 text/picturebook/movie/experience(hidden)/친구복사:3004), ui.js:711 `selectProjectType`, :740·1362 data-ptype 바인딩.

```
┌─────────────────────────────────────────────┐
│  📖 그림책 만들기                            │   ← 대영역(위계 상단)
│  ┌───────────┬───────────┬───────────┐      │
│  │ 1단계      │ 2단계      │ 3단계      │      │
│  │ 나의 동화책 │ 도움을 받아 │ 갈림길 이야기│      │
│  │ 을 만들어  │ 만들어봐요  │ 를 스스로의 │      │
│  │ 줘요 (1·2) │ (3·4)     │ 힘으로(4~6) │      │
│  └───────────┴───────────┴───────────┘      │
├─────────────────────────────────────────────┤
│  다른 방법  ▸ ✏️ 글 이야기  ▸ 🎬 무비형        │   ← 소카드(위계 하단)
│            ▸ 📋 친구 작품 복사               │
└─────────────────────────────────────────────┘
```

- 구현: 그리드 재구조화 + 그림책 3카드는 `data-ptype="picturebook" data-pblevel="1|2|3"`. 핸들러는 기존 data-ptype forEach에 pblevel 캡처만 추가 → `selectProjectType('picturebook', {level})`.
- 재진입(기존 작품): 기존 W7 로직(viewer-meta/projectType 조회→"이전 선택" 강조)에 picturebookLevel 반영 — 해당 단계 카드 강조.
- 학년 라벨은 **안내일 뿐 강제 아님**(교사 재량).

## 6. 단계별 흐름도

```
[1단계]  진입(1단계 선택) → 나침반 easy(4~5문항)
         → [AI] 글 전체 초안 생성 → 일직선 6~8장면 자동 배치(제목·표지 포함)
         → 다듬기 진입 시 [AI] 그림 없는 장면 일괄 text→image + 말풍선 상단밴드 프리셋
         → 아이: 글자 위치 조정·문구 소수정  → 감상/인쇄/책장 (고쳐쓰기 스킵)

[2단계]  진입(2단계 선택) → 나침반 v2(현행 10문항)
         → [AI] 장면별 이야기 초안 배치(일직선·분기 UI 잠금)
         → 아이: 초안을 자기 글로 고쳐 씀 → 그림 직접(그리기/업로드/AI마감 현행)
         → 고쳐쓰기·작품검사 현행  → 감상/인쇄/책장

[3단계]  현행 그림책 흐름 그대로 (빌드 0 · 라벨만)
```

## 7. 구현 설계 (컴포넌트별 재사용 맵)

| # | 항목 | 방법 | 난이도 | 층 |
|---|---|---|---|---|
| 7.1 | 진입 재구조화 + picturebookLevel | §5. maker.html+ui.js+firebase.js 편승 | 쉬움 | 클라 |
| 7.2 | 일직선 잠금 (1·2단계 공용) | maker에서 분기 UI(선택지 추가/연결) 숨김·buttons는 '다음' 1개 자동. 뷰어는 무변경(일직선=분기의 특수형) | 중 | 클라 |
| 7.3 | 나침반 easy (1단계) | 질문세트 easy 신설(4~5문항: 주인공·장소·사건·기분). `resolveQuestionSetVersion`(thought-compass.js:160) 확장. 소비자는 7.4 콜러블뿐 — 기존 v2 3곳 동기화(s2·검사·질문AI) 경로와 **분리** | 중 | 클라 |
| 7.4 | 🔴 AI 글 초안 콜러블 (1·2단계 공용) | `teacherScriptDraft`(functions/index.js:2949) 파이프라인 재사용 → 학생판 `studentStoryDraft`(가칭): 입력=나침반 답변+level, 게이트=팀 멤버십+학급 AI 설정+일일한도(ai-usage 패턴), 출력=**일직선 N장면 JSON**(1단계=전문·2단계=초안 톤). 클라 적용부는 adminConsole.js 대본 적용 로직 참고 — ⚠️장면 스키마 4곳 동기화 준수. **scenes 원본 쓰기 = 빈 작품 전제 + 기존 본문 존재 시 덮어쓰기 금지 가드** | 중~큼 | 함수+클라 |
| 7.5 | 🔴 text→image (1단계) | image-s2-adapter-openai.js `ENDPOINT=/v1/images/edits`(:18, 원본그림 필수) → **`/v1/images/generations` 경로 추가**(같은 gpt-image-2·인증/출력/스타일·P8 mood 재사용). 결과는 **기존 s2 변형 슬롯(aiVariants/image)에 저장** → AI-DEFAULT-VIEW-1이 감상 기본으로 자동 표시·인쇄도 따라감 = 뷰어 무변경. 재생성은 기존 🔁 forceRegenerate 재사용. 1단계는 그리기/업로드 UI 자체를 숨겨 sourceMode 충돌 원천 차단 | 중 | 함수+클라 |
| 7.6 | 말풍선 자동 배치 (1단계) | **§8 PoC 관문**. 통과 시: 생성 시점에 `scene.picturebookBodyBox`를 상단 밴드 좌표로 프리셋(viewer-render.js:756~828 렌더 경로 무변경·아이의 배치 조정 기능 그대로) | PoC | 클라(+프롬프트) |
| 7.7 | 고쳐쓰기 스킵 (1단계) | HUD/더보기(REFINE-IA-3)의 고쳐쓰기 진입을 level=1일 때 숨김 | 쉬움 | 클라 |

## 8. 말풍선 자동 배치 PoC — 1단계의 관문 (GO/NO-GO)

- **가설**: 얼굴인식 ML 없이 **구도 통제**로 결정적 해결 — 이미지 프롬프트에 "주인공은 하단·중앙, **상단 1/3은 하늘/배경으로 비워** 글자리 확보" 지시 + 글상자는 상단 밴드 고정.
- **방법**: 프로덕션 어댑터 모듈 직접 라운드트립(P8 검증과 동일 방식·학생 데이터 무기록) — generations 엔드포인트로 대표 장면 문장 10종 × 1장.
- **GO 기준**: 10장 중 **8장 이상**에서 상단 밴드(글상자 영역)가 인물 얼굴/핵심 피사체와 겹치지 않음.
- **NO-GO 폴백**: 글상자 **하단 안전밴드 고정**(구도 지시를 "인물 상단 배치"로 반전) — §11-① 사용자 수용 확정.

### ✅ PoC 결과 (2026-07-18 실측) — **GO (9/10 PASS)**
- 실행: gpt-image-2 `/v1/images/generations` 10장(1536x1024·medium·webp), 스타일=어댑터 P7/P8 DNA 승계, 구도 지시="인물·얼굴·핵심 피사체는 하단 2/3, 상단 1/3은 저디테일 여백". 산출물=`~/Desktop/가지_말풍선PoC_20260718/`(판정 시트 contact-sheet.html 포함). 비용 ~$0.5.
- **hard case 4종 중 3종 통과**: 기린(목을 옆으로 굽혀 머리를 밴드 아래로)·나무 위 둥지(둥지를 중단 배치)·무지개(아치를 밴드 아래로). 군집(생일 6명)도 고깔까지 전부 밴드 아래.
- **유일 FAIL=연날리기**: 얼굴은 지켰으나 '하늘에 떠야 하는 소재'(연)가 밴드 침범 — 소재 자체가 상단으로 가는 부류(연·풍선·불꽃놀이)의 구조적 한계.
- **수용 근거**: 이 ~10% 케이스는 1단계에서 "아이가 하는 일"(글자 위치 조정)과 정확히 맞물림 — 글상자를 아래로 내리면 해결·저장 구조 동일(picturebookBodyBox). 별도 ML 불요.
- 부수 확인: 10장 전부 글자/워터마크 0(no-text 가드 유효)·밝은낮·수채+색연필 스타일 일관.

## 9. 비용·쿼터 게이트 (1단계 자동 그림) — ✅결정: 팀당 총량

- 추정: 책당 표지+6~8장 × $0.05 ≈ **$0.4/권**.
- **결정(07-18)**: **팀당 총량 제한** — 팀(모둠)당 자동 일괄 생성 1회 + 장면별 수동 🔁 재생성 총 N회(초안 N=12·상한 도달 시 안내). `ai-usage` 트랜잭션 패턴을 팀 노드에 적용 + 학급 AI 설정(교사 on/off) 게이트 통과 필수. 팀별 형평 명확·반 전체 폭주는 학급 AI 설정 off로 대응.

## 10. 2주 일정 (07-18 기준 · 제출 D-14)

| 주차 | 항목 | 게이트 |
|---|---|---|
| **주1** (07-18~24) | ① 진입 재배치+picturebookLevel(3단계=현행 배선) — 즉시 위계·데모 가능 ② **말풍선 PoC** ③ 2단계(일직선 잠금+나침반→초안 콜러블) | ②GO/NO-GO · ③함수 배포 전 승인 |
| **주2** (07-25~31) | ④ 1단계 풀오토(easy 나침반+글 자동+generations+자동 말풍선+고쳐쓰기 스킵) ⑤ 쿼터 게이트 ⑥ 실기기(크롬북) 검증+데모 리허설 | ④함수 배포 전 승인 · 버퍼 2일 |

최대 리스크=④(의존: ②③⑤ 전부). 대회 발표의 8할 = **진입 사다리 화면 + 1단계 라이브 데모**.

## 11. 결정 사항 (2026-07-18 사용자 확정)

1. **말풍선 폴백**: ✅수용 — PoC 실패 시 "글상자 하단 안전밴드 고정"으로 진행(일정 리스크 최소화).
2. **비용 상한**: ✅**팀당 총량**(§9) — 반 단위 일일 쿼터 기각.
3. **1단계 구조**: ✅**일직선 6~8장면** — 분기 동화책 기각(사다리 서사·생성 안정성·비용 고정).

## 12. 안전·리스크 분류 (운영 모드 매핑)

- 🔴 **안전 모드**(조사·구현 분리·배포 전 승인): 7.4 콜러블 신설(+scenes 원본 쓰기 가드), 7.5 어댑터 generations 경로. rules 변경은 **없음**(기존 노드만 사용).
- **표준 모드**: 7.1~7.3, 7.6, 7.7 (클라 비계·뷰어 무변경).
- 회귀 면적: 뷰어/인쇄/책장 0(원칙) · 3단계 0(라벨만) · 진입 그리드는 기존 data-ptype 핸들러 보존.
- 기존 작품 보호: picturebookLevel 없음=3단계, AI 초안은 빈 작품 전제 가드, "0000"(JL26A)·연습반 데이터 무접촉.

## 13. 검증 계획

- PoC: 실 어댑터 라운드트립 10장 실측(§8).
- 클라: 하니스(레벨 분기·일직선 잠금·easy 세트)+`node --check`+버스터 `--check`, 로컬 8000(picturebook-repo main) 실측.
- 함수: focused 하니스(초안 스키마·게이트) → 배포 승인 → 연습반(학급코드 0000 아님 주의) 실계정 e2e.
- 실기기: 크롬북 1단계 전 흐름 1회 + 인쇄 1회.

## 14. LEVELS-CONT — 2단계 "이어쓰기" 개편 (2026-07-18 사용자 확정 · 구현 완료)

고쳐쓰기 모델 폐기 → **AI가 시작 3장면만 쓰고 아이가 이어서 완성**. 사다리가 "AI 전부(1) → 시작만(2) → 아이 전부(3)"로 유기화.

### 14.1 확정 사양 → 구현
1. **8장면 고정**: linear 나침반 세트(v4)에서 targetLength 질문 제거 → **9문항**. `CORE_QUESTION_KEYS_LINEAR` 2곳 동기(thought-compass-questions.js·thought-compass.js — validator/완주는 키배열 기준 자동). `resolveStoryCount`는 targetLength 없으면 8이라 정합(함수 자체는 v2 공용이라 유지). 구 v4 세션(targetLength 답 잔존)도 9키만 채우면 완주(하위호환).
2. **씨앗 힌트**: `studentStoryDraft` level 2 출력 = 장면 1~3(BASE10 키 2~4) 완성문 `body` + 장면 4~8(키 5~9)·엔딩(키 10) **`hint` 한 문장**(물음형). 서버 sanitize가 결정적 강제(앞 3장면 hint 버림·4장면부터 body 버림 — AI 슬립에도 "AI는 시작만" 유지). 클라 `_applyStoryDraftStarter`가 `scene.writingHint`(≤140자)로 기록.
   - **writingHint = 메이커 전용 신규 필드**: 카드 미리보기(`💡 힌트`·empty 스타일)·카드 textarea placeholder·퀵에디트 placeholder로만 노출. 글을 쓰면 사라지고 지우면 다시 보임(placeholder 원칙). 감상/인쇄/AI 스냅샷은 body만 읽어 **뷰어 무변경 원칙 유지**. rules 변경 불요(scenes write auth 게이트만).
3. **그림 현행 유지**: 2단계 자동 그림 없음(아이 그리기+AI 그림책 마감) — 글·그림 대칭.
4. **[✅ 내 글 점검받기]** (2단계 다듬기 HUD·📔 자리): `viewerAi.startCheckLite()` = 작품검사 **단독** 실행(온보딩/준비게이트/생각점검/마지막다듬기 없음). 결과 모달은 2단계에서 쉬운 말 카피(제목 "✅ 내 글 점검"·branchFlow 라벨 "이야기 마무리 확인"·작품마무리 언급 제거) — `_showLatestWorkCheck` 재사용이라 최근 결과 보기도 동일. openModal은 2단계 안내 문구로 2중 방어.
   - **일직선 검사 소음 2중 차단**: ①클라 스냅샷이 2단계에선 choices 미전송(base10 `choiceA=''`가 어댑터에서 빈 라벨로 읽혀 "선택지 텍스트 비어있음" ×장면수 나오던 것) ②서버 `callWorkCheck`가 `viewer-meta/picturebookLevel`을 직접 read(1·2단계=linearLocked)해 `buildUserMessage` 4번째 인자(opts.linearLocked·미전달=바이트 동일)로 "선택지/분기 추가 권고 금지·마무리만" 블록 삽입. e2e 3차 검사에서 분기 권고 0·실질 지적("결말 구체화")만 확인.
5. **문구**: 진입 카드 desc "AI가 이야기를 시작해 주면 내가 이어서 완성해요 · 3~4학년" · 환영/다듬기 슬라이드 pbLevels:[2] 교체(+refineWelcome에 ✅ 슬라이드 신설·📔 슬라이드 pbLevels:[3]) · 초안 토스트 "AI가 이야기를 시작해 줬어요…".

### 14.2 함께 잡은 기존 갭 2건
- **튜토리얼 pbLevels 필터 무력화**: maker 환영 호출 2곳(ui.js `_enterMakerAfterPtypeSelected`·thought-compass-review.js `_complete`)이 `filterType` 미전달 → 유형/단계 필터가 항상 3단계로 평가(1·2단계에 갈림길 서사 환영 노출). filterType 전달로 정정. viewer에는 `getPicturebookLevel` shim 신설(viewer-data — ViewerState 기반·maker 정의 존중)로 다듬기 환영/코치도 단계 필터 동작.
- **나침반 완료 version 스탬프**: review `_complete`의 qVersion 파생이 targetLength 유무 이분법(1|2)이라 easy/linear 완료가 version 1로 저장되고 답 기반 자가복구에 기대던 것 → heroWho=3·protagonistName=4 명시 파생으로 정정.

### 14.3 검증 (2026-07-18)
- 하니스: 나침반 240/240(9문항 계약 갱신)+write-after 29 · 서버 sanitize/validate 함수추출 하니스 5종(level1 불변·씨앗힌트·슬립 강제·결함 검출·프롬프트) · buildUserMessage 바이트동일 계약 3종.
- 브라우저 실측: linear 9문항 라이브·카드 💡 미리보기/placeholder/퀵에디트·본문 쓰면 힌트 소멸·HUD 매트릭스(1단계=없음·2단계=✅만·3단계=📔만)·lite 모달 카피·3단계 카피 회귀 0.
- **실계정 e2e(연습반 신규 팀 '이어쓰기테스트'·PIN 4321·account 노드 CLI 생성)**: 2단계 선택(confirm 라벨) → linear 9문항 실답(AI 후속 '이름' 강제 실작동) → `studentStoryDraft` 실호출 → RTDB scenes = 표지 제목 '털볼이와 심술쟁이 까치' + 키2~4 body + **키5~9·10 writingHint**(body '') 실확인 · aiUsage.storyDraft=1 → 장면5·6·7 이어쓰기(퀵에디트 placeholder=힌트 전문) → [✅ 내 글 점검받기] 실 Haiku 3회(1차 lite 카피/2차 choices 미전송/3차 linearLocked 프롬프트 — 분기 소음 0·유용 지적 1건) → 환영 필터 fix 실측(2단계 슬라이드).
- 배포: `studentStoryDraft`·`callWorkCheck`(asia-northeast3) 재배포. 기존 테스트 팀(이단계테스트·이단계테스트2)은 옛 방식 데이터 그대로(신규만 새 방식).

### 14.4 LEVELS-CONT-B — 위치 기반 씨앗 힌트로 교체 (2026-07-18 사용자 결정 · 구현·배포·e2e 완료)

사용자 문제 제기: 힌트가 초안 생성 시점에 고정된 "전개 예언"이라, 아이가 장면 6을 힌트와 다르게 쓰면 7~8 힌트가 어긋남(꼬임). → **B안 채택**: 힌트의 성격을 "전개 예언"에서 "이야기 위치 안내 + 아이 자신의 나침반 답 되비추기"로 교체. 내용을 예언하지 않으니 구조적으로 안 꼬이고, '시키는 말'이 아니라 '네 계획 되짚기'가 됨.

- **서버 단순화**: `studentStoryDraft` level 2 = **시작 3장면만**(scenes 정확히 3·엔딩 빈 문자열·hint 출력 폐기). sanitize가 과잉 출력(4장면 이상·엔딩 완성문·hint 잔재)을 결정적으로 절단. level 1 불변. 시작 3장면 프롬프트에 "큰 사건의 해결로 나아가지 말고 여지를 넓게" 지시.
- **클라 생성**: `_buildLinearSeedHints(answers)`(mobileTextBranch·순수 함수·window 노출) — 키 5(어려움↑·«risingTrouble»)·6(주인공 마음·«protagonist»)·7(중요한 선택·«keyChoice»)·8(선택 다음·인용 없음)·9(마무리 길·«trueEnding»·"다른 길로 가도 좋아")·10(마지막 장면·«coreMessage»). 답 유예/결측=인용 없이 위치 문구만. 인용 36자 절단·힌트 140자 상한. `_applyStoryDraftStarter`가 level 2 빈 장면에만 얹음(구서버가 hint를 보내와도 무시 — 전후방 호환).
- **"힌트는 참고" 문구**: ①퀵에디트 모달(힌트 표시 상태)="💡 힌트는 참고만! 내 생각대로, 힌트와 다른 방향으로 써도 좋아요." ②환영 슬라이드(pbLevels 2)="힌트는 참고일 뿐 — 다른 방향으로 써도 좋아요." ③힌트 9 자체에 "그대로 가도, 다른 길로 가도 좋아" 내장.
- **검증**: 서버 함수추출 하니스 5종(level1 불변·3장면 계약·과잉 절단·결함 검출·프롬프트)+나침반 240/240. 브라우저 스텁 3경로(신서버 3장면/구서버 8장면+hint 호환/level1 무힌트)+힌트 빌더(정상·유예·결측)+퀵에디트 문구 2상태. **실계정 e2e '이어쓰기테스트2'**: 나침반 9문항 → 신계약 초안(AI 3장면이 해결 전에 멈춤) → RTDB 키2~4 body·키5~9/10 위치 힌트+«아이 답 인용» 실확인·aiUsage.storyDraft=1. `studentStoryDraft` 재배포. 기존 팀(이어쓰기테스트)의 구식 AI 힌트 데이터는 그대로(렌더 동일).

### 14.5 LEVELS-AUDIT — 1·2단계 10기준 검증과 F1~F7 수정 (2026-07-18)

10기준(구조잠금·HUD매트릭스·마무리차단·나침반배선·초안게이트·자동그림경계·writingHint격리·회귀·문구정합·코드건전성) 검증 → 발견 7건 전부 수정·라이브 실측:
- **F1** 2단계 [🖼️ AI 그림책 마감] HUD 직접 진입 신설(작품 마무리 모달 폐쇄로 유일 진입점이 닫혀 있던 것 복구·패널 자체 게이트 재검증·`js-ai-imgs2-open`). 그림 토글 바와 정합.
- **F2** 다듬기 일직선 잠금(`_isLinearLockedViewer`): 🔗 팝오버·우측 패널에서 버튼 추가/삭제/연결(nextId) 변경 차단 — 렌더 숨김/disabled + 핸들러·헬퍼(_pbAdd/RemoveAt/RemoveLast) 2중 가드. 라벨 편집(무대 인라인)은 유지. 팝오버에 🔒 안내문.
- **F3** 1·2단계 🗑 전체 초기화 숨김(CSS)+clearAll 가드 — 초기화 시 초안 재생성 트리거 부재로 복구 불가하던 함정 제거.
- **F4** refine 환영 덱 단계 정합: 🖼️ 슬라이드 pbLevels [2,3]·🔗 정리 [3]+2단계 전용 '🔗 버튼 글자 바꾸기' 신설. 1단계 덱=6장(그림/버튼/마무리 언급 0) 실측.
- **F5** 2단계 글 보기 토글(원본|AI 장면발전) — s2 도달 불가라 죽은 UI → 변형 없으면 숨김(변형 존재 예외작품은 보기 전용 유지).
- **F6** ⚙️ 시작점 1·2단계 숨김(CSS·코치는 가시성 가드로 자동 스킵).
- **F7** 카드 일반/엔딩 라디오 1·2단계 숨김(CSS)+updateType 관문 가드.
- 검증: 합성 매트릭스(lv2 숨김/가드·lv3 전부 복원)+이어쓰기테스트2 라이브(HUD ✅+🖼️·패널 게이트 문구·팝오버 잠금+select disabled+value 유지·글 토글 소멸·그림 토글 유지)·테스트 269/269·버스터. PASS 항목·잔여 관찰(choiceA 라벨 불일치=3단계 공통)은 메모리 levels 토픽 참조.
