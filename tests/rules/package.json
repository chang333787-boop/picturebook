{
  "name": "branch-rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "RTDB security rules tests (Database Emulator). 운영 미연결 — fixture만 사용.",
  "scripts": {
    "test": "firebase emulators:exec --only database --project demo-branch-rules \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.15.0"
  }
}
